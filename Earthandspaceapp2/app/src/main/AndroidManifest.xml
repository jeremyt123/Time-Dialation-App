<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jeremy.earthandspaceapp2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".speedSelection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Destinationselector"
            android:label="@string/title_activity_destinationselector"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DestinationSelectorBasic"
            android:label="@string/title_activity_destination_selector_basic"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DestinationSelectorEmpty" />
        <activity android:name=".SunFacts"></activity>
    </application>

</manifest>